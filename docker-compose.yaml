version: '3'

services:
  web:
    build: ./web
    volumes:
      - ./web:/usr/src/app
    ports:
      - 5000:8080

  node-server:
    build: ./server
    volumes:
      - ./server:/usr/src/app
    environment:
      - APP_PORT=8080
    ports:
      - 5001:8080
